{
  "name": "nuxt-koa-project",
  "version": "1.0.0",
  "description": "My top-notch Nuxt.js project",
  "author": "dino",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "pm2":"nuxt build && pm2 start ecosystem.config.js --only prod"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "cross-env": "^5.2.0",
    "koa": "^2.6.2",
    "less": "^3.9.0",
    "less-loader": "^5.0.0",
    "md5": "^2.2.1",
    "mint-ui": "^2.2.13",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
